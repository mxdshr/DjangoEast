language: python
sudo: enabled
services:
    - mysql
python:
    - "3.6"
env:
    global:
     - TRAVIS=true
before_install:
    - mysql -e 'CREATE DATABASE django_east CHARACTER SET utf8 DEFAULT COLLATE utf8_general_ci;'
#    - mv configs.example.json configs.json
# command to install dependencies
install:
    - pip install -r requirements.txt
script:
    - python manage.py makemigrations
    - python manage.py migrate

